{
  "name": "demo-folder-hack",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-node": "npx esbuild src/index.js --bundle --platform=node --target=node18 --outfile=dist-node/index.js --format=cjs",
    "build-cloudflare": "npx wrangler build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "wrangler": "^3.15.0"
  },
  "dependencies": {
    "esbuild": "^0.19.7"
  }
}
